
<div class="jumbotron">
  <h1 class="display-4"><%= @book.name %>-</h1>
  <p class="lead"><%= @book.author_name %> | <%= @book.price %></p>
  <br/>
  <%= link_to 'Edit', edit_book_path(@book), class:"btn btn-dark" %>
  <%= link_to 'Delete', @book, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-danger" %> 
<%= link_to 'Back', books_path, class:"btn btn-secondary" %>
</div>